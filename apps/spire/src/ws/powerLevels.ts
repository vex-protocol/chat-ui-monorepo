/** Power level required to send an invite. */
export const INVITE = 25

/** Power level required to create a resource (channel, server, etc.). */
export const CREATE = 50

/** Power level required to delete a resource. */
export const DELETE = 50

/** Power level required to manage custom emoji. */
export const EMOJI = 25
